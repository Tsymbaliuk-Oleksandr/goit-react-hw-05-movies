import Box from 'components/Box';
import Navigation from 'components/Navigation';

const AppBar = () => {
  return (
    <Box as="header">
      <Navigation />
    </Box>
  );
};

export default AppBar;
